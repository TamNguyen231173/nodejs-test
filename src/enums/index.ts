
export enum ERole {
  ADMIN = "ADMIN",
  USER = "USER"
}

export enum EToken {
  VERIFY = "VERIFY",
  FORGOT_PASSWORD = "FORGOT_PASSWORD",
  ACCESS_TOKEN = "ACCESS_TOKEN",
  REFRESH_TOKEN = "REFRESH_TOKEN"
}
